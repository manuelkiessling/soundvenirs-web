<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Soundvenirs{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body
        {% if ng_app is defined %}
            ng-app="{{ ng_app | escape('html_attr') }}"
        {% endif %}
        >

        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
